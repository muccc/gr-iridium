# auto-generated by grc.converter

id: iridium_iridium_qpsk_demod_cpp
label: iridium_qpsk_demod_cpp
category: '[Iridium]'

parameters:
-   id: channels
    label: Channels
    dtype: int
    default: '1'

inputs:
-   domain: message
    id: cpdus
    multiplicity: ${channel}

outputs:
-   domain: message
    id: pdus

templates:
    imports: import iridium
    make: "iridium.iridium_qpsk_demod_cpp(${channels})"

file_format: 1
